[dependencies]
anyhow = "1"
clap = {version = "4", features = ["derive", "env"]}
simpaas-core = {path = "../core"}
tokio = {version = "1", features = ["full"]}

[package]
description = "SimPaaS API"
name = "simpaas-api"
edition.workspace = true
version.workspace = true
